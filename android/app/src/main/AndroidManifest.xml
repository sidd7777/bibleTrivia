<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bible_trivia">

    <!-- Required Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" />

    <application
        android:label="bible_trivia"
        android:icon="@mipmap/flutter_new_logo"
        android:theme="@style/LaunchTheme"
        android:enableOnBackInvokedCallback="true">


        <!-- Meta-data for Google Sign-In -->
        <meta-data
            android:name="com.google.android.gms.auth.api.signin.DEPENDENCIES"
            android:value="oauth2:https://www.googleapis.com/auth/userinfo.email" />
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <!-- Meta-data for Facebook SDK -->
        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id" />
        <meta-data
            android:name="com.facebook.sdk.LoginProtocolScheme"
            android:value="@string/fb_login_protocol_scheme" />
        <meta-data
            android:name="com.facebook.sdk.ClientToken"
            android:value="@string/facebook_client_token" />

        <!-- Facebook Login Callback Activity -->
        <activity
            android:name="com.facebook.FacebookActivity"
            tools:replace="android:configChanges"
            android:configChanges="keyboard|keyboardHidden|orientation|screenSize|screenLayout"
            android:label="@string/app_name" />

        <!-- Facebook Custom Tab Activity for Redirects -->
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <!-- Add Facebook Login Callback -->
        <activity
            android:name="com.facebook.FacebookCallback"
            android:exported="true">
            <intent-filter>
                <action android:name="com.facebook.platform.PLATFORM_ACTIVITY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- MainActivity Configuration -->
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:theme="@style/NormalTheme"
            android:exported="true">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <queries>
        <!-- Declare the Facebook app package to allow interactions -->
        <package android:name="com.facebook.katana" />
    </queries>
</manifest>
